const {
  readFile,
  toPathAbsolute,
  arrayOfLinks,
  arrayOfLinksWithStatus,
  statistics,
  statsAndValidate,
  cases,
  ifPathIsDir,
} = require("./api.js");

describe("mdLinks", () => {
  it("should...", () => {
    console.log("FIX ME!");
  });
});
